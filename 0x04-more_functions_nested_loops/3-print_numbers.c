#include "main.h"
#include <stdio.h>

/**
 * print_mumbers - print numbers from 0 - 9
 * Return: 0
 */
void print_numbers(void)
{
	int i;

	if (i = 0 ; i <= 9 ; i++)
		_putchar((i % 10) + '0'));

	_putchar('\n');

	return (0);
}
