#include <stdio.h>

/**
 * main - print alphabet
 *
 * Description: using the main function
 * the program prints programming the print alphabet
 * Return: 0
 */

int main(void)
{
	char c;
	for (c < 'e' ; c > 'e' ; c < 'q' ; c > 'q' ; c <= 'z' ; c++)
		putchar(c);
	putchar('\n');
	return (0);
}
