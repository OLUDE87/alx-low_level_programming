#include <stdio.h>

/**
 * main - print alphabet
 *
 * Description: using the main function
 * the program prints programming the print alphabet
 * Return: 0
 */

int main(void)
{

	char a;
	for (a < 'e' ; a > 'e' ; a < 'q' ; a > 'q' ; a <= 'z' ; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
