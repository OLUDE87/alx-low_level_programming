#include "main.h"

/**
 * _isdigit - check digits from 0 through 9
 * @C: argument digits to check
 * Return: 1 is c is a digits, 0 otherwise.
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9' c++)
		return (1);
	else
		return (0);
}
